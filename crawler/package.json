{
  "name": "rubinot-kill-stats-crawler",
  "version": "1.0.0",
  "description": "Daily crawler that captures /api/killstats for every RubinOT world and writes JSON to ../data",
  "private": true,
  "main": "crawl.js",
  "scripts": {
    "crawl": "node crawl.js"
  },
  "dependencies": {
    "playwright": "1.49.0"
  }
}
